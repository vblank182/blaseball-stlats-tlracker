language: python
python:
    - "3.9"
install:
    - pip install -r requirements.txt
    - pip install .
script:
    - sh ls -lash
    - sh ls -lash blaseball-stlats-tlracker
    - sh ls -lash tests
    - python tests/tests.py
